import React from 'react';
import PropTypes from 'prop-types';

const Footer = props => {
    return (
        <div>
            Footer
        </div>
    );
};

Footer.propTypes = {
    
};
Footer
export default Footer;Footer