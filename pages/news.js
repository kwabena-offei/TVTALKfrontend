import React from 'react';
import PropTypes from 'prop-types';

const news = props => {
    return (
        <div>
            News
        </div>
    );
};

news.propTypes = {
    
};

export default news;